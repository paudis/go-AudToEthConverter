An application written in Go that demonstrates concurrency, interfaces and error handling. 

This application will convert an amount from AUD to ETH using multiple APIs concurrently to aggregate the data